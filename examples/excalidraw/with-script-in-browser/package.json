{
  "name": "with-script-in-browser",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "@notud/excalidraw": "*"
  },
  "devDependencies": {
    "vite": "5.0.12",
    "typescript": "^5"
  },
  "scripts": {
    "start": "yarn workspace @notud/excalidraw run build:esm && vite",
    "build": "yarn workspace @notud/excalidraw run build:esm && vite build",
    "build:preview": "yarn build && vite preview --port 5002"
  }
}
